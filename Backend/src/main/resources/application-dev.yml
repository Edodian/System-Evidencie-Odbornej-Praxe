spring:
  datasource:
    url: jdbc:mariadb://mariadb:3306/sep_dev
    username: appuser
    password: apppassword
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create

keycloak:
  base-url: http://dev-keycloak:8080
  realm: sep
  client-id: sep-backend
  client-secret: dev-secret-123
  admin-username: admin
  admin-password: admin
